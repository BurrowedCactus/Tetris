#include <cstddef>

//Grid
extern const size_t WIDTH;
extern const size_t HEIGHT;
extern const size_t BUFFER_HEIGHT;

//Block
// typical number of cells in a block
extern const size_t BLOCK_SIZE;

//Graphic display
// horizontal space excluding the main grid and the area for next block
extern const int BUFFER_STR;
// space reserved for the edge around the next block
extern const int BUFFER_NEXTBLOCK;
// height(position) of the next box
extern const int HEIGHT_NEXT;

extern const int WIN_SIZE;

extern const int ALPHABET;
