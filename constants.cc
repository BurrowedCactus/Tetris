#include "constants.h"

//Grid
const size_t WIDTH = 11;
const size_t HEIGHT = 15;
const size_t BUFFER_HEIGHT = 3;

//Block
// typical number of cells in a block
const size_t BLOCK_SIZE = 4;

//Graphic display
// horizontal space excluding the main grid and the area for next block
const int BUFFER_STR = 10;
// space reserved for the edge around the next block
const int BUFFER_NEXTBLOCK = 2;
// height(position) of next block area
const int HEIGHT_NEXT = 2;

const int WIN_SIZE = 500;

const int ALPHABET = 26;
